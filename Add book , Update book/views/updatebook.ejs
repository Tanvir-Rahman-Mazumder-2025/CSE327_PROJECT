<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <title>Update Book</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 font-sans min-h-screen">



    <div class="flex flex-col items-center p-10">
        <h1 class="text-4xl font-bold mb-10 text-gray-800">Update Book</h1>

        <form action="/update_book/<%= book._id %>" method="POST"
            class="bg-white p-10 rounded-2xl shadow-lg w-full max-w-2xl space-y-6">


            <div>
                <label for="title" class="block text-lg font-semibold mb-2">Title</label>
                <input type="text" name="title" id="title" value="<%= book.title %>" required
                    class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500" />
            </div>


            <div>
                <label for="author" class="block text-lg font-semibold mb-2">Author</label>
                <input type="text" name="author" id="author" value="<%= book.author %>" required
                    class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500" />
            </div>


            <div>
                <label for="price" class="block text-lg font-semibold mb-2">Price (৳)</label>
                <input type="number" name="price" id="price" value="<%= book.price %>" required step="0.01" min="0"
                    class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500" />
            </div>


            <div>
                <label for="description" class="block text-lg font-semibold mb-2">Description</label>
                <textarea name="description" id="description" rows="4"
                    class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500"><%= book.description %></textarea>
            </div>


            <div>
                <label for="category" class="block text-lg font-semibold mb-2">Category</label>
                <input type="text" name="category" id="category" value="<%= book.category %>" required
                    class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500" />
            </div>


            <div>
                <label for="language" class="block text-lg font-semibold mb-2">Language</label>
                <input type="text" name="language" id="language" value="<%= book.language %>" required
                    class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500" />
            </div>


            <div>
                <label for="image_url" class="block text-lg font-semibold mb-2">Image URL</label>
                <input type="url" name="image_url" id="image_url" value="<%= book.image_url %>"
                    class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500" />
            </div>


            <div>
                <label for="availavility_status" class="block text-lg font-semibold mb-2">Availability Status</label>
                <select name="availavility_status" id="availavility_status"
                    class="w-full border border-gray-300 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-blue-500">
                    <option value="available" <%=book.availavility_status==='available' ? 'selected' : '' %>>Available
                    </option>
                    <option value="unavailable" <%=book.availavility_status==='unavailable' ? 'selected' : '' %>
                        >Unavailable</option>
                </select>
            </div>


            <div class="flex justify-center">
                <button type="submit"
                    class="bg-blue-500 text-white text-lg font-semibold px-8 py-3 rounded-xl hover:bg-blue-600 transition">
                    Update Book
                </button>
            </div>
        </form>
    </div>

</body>

</html>